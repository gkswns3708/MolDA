# toy set: 빠른 검증용 100-sample split
tag: toy100
root: dataset               # New_MolDA/dataset/ 기준 상대 경로

# train.py에서 root를 절대 경로로 변환 후 조합:
#   train_path = os.path.join(project_root, root, splits.train)
splits:
  train: Train_toy100       # 2,100 rows (21 tasks × 100)
  val: Val_toy100           # 1,900 rows
  test: Test_toy100         # 1,900 rows

# data processing (default.yaml에서 상속, 여기서 override 가능)
# max_length: 512
# gen_max_len: 256
