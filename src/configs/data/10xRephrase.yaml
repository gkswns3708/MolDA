# Train = raw_v1 + 10x rephrase; Val/Test = raw_v1 복사본 (hardlink)
# Alias of raw_v1_10x_rephrase with shorter tag/name. Same on-disk root.
tag: 10xRephrase
root: raw_v1_10x_rephrase

splits:
  train: Train
  val: Val
  test: Test
